<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ModernMath" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ic_home_background</item> <!-- buttons -->
        <item name="colorPrimaryVariant">@color/pv</item>
        <item name="colorOnPrimary">@color/black</item> <!-- text on buttons -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item> <!-- FAB, selected options-->
        <item name="colorSecondaryVariant">@color/sv</item>
        <item name="colorOnSecondary">@color/black</item> <!-- text on FAB -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>