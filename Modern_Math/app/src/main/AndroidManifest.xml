<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.modernmath">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mm"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mm_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ModernMath">
        <activity
            android:name=".login.AddExistingUserActivity"
            android:exported="false" />
        <activity
            android:name=".login.AddNewUserActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.UserLoginCard"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>